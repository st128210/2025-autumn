name: Windows CI

on: [push, pull_request]

env:
  VCPKG_ROOT: "${{ github.workspace }}/vcpkg"
  CMAKE_TOOLCHAIN_FILE: "${{ github.workspace }}/vcpkg/scripts/buildsystems/vcpkg.cmake"

jobs:
  build-and-test:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup vcpkg 
      shell: pwsh
      run: |
        git clone https://github.com/Microsoft/vcpkg.git
        .\vcpkg\bootstrap-vcpkg.bat
        .\vcpkg\vcpkg install gtest:x64-windows
        
    - name: Build HW1 
      shell: pwsh
      run: |
        cd 2025.09.20_HW1
        cmake -B build -S .
        cmake --build build --config Release
        $exe = Get-ChildItem -Path build -Recurse -Filter "hw1.exe" | Select-Object -First 1
        if ($exe) { & $exe.FullName } else { Write-Error "hw1.exe not found" }
    
    - name: Build HW2 
      shell: pwsh  
      run: |
        cd 2025.09.28_HW2
        cmake -B build -S .
        cmake --build build --config Release
        $exe = Get-ChildItem -Path build -Recurse -Filter "hw2.exe" | Select-Object -First 1
        if ($exe) { & $exe.FullName } else { Write-Error "hw2.exe not found" }

    - name: Build HW3 
      shell: pwsh  
      run: |
        cd 2025.10.04_HW3
        cmake -B build -S .
        cmake --build build --config Release
        $exe = Get-ChildItem -Path build -Recurse -Filter "hw3.exe" | Select-Object -First 1
        if ($exe) { & $exe.FullName } else { Write-Error "hw3.exe not found" }
        
    - name: Build HW4 
      shell: pwsh  
      run: |
        cd 2025.10.11_HW4
        cmake -B build -S .
        cmake --build build --config Release
        $exe = Get-ChildItem -Path build -Recurse -Filter "hw4.exe" | Select-Object -First 1
        if ($exe) { & $exe.FullName } else { Write-Error "hw4.exe not found" }
        
    - name: Build TextEditor 
      shell: pwsh  
      run: |
        cd 2025.10.18_HW5/TextEditor
        cmake -B build -S . -DCMAKE_TOOLCHAIN_FILE="${{ env.CMAKE_TOOLCHAIN_FILE }}"
        cmake --build build --config Release
        $exe = Get-ChildItem -Path build -Recurse -Filter "text_editor.exe" | Select-Object -First 1
        if ($exe) { & $exe.FullName } else { Write-Error "text_editor.exe not found" }
        
    - name: Build BankAccount 
      shell: pwsh  
      run: |
        cd 2025.10.18_HW5/BankAccount
        cmake -B build -S . -DCMAKE_TOOLCHAIN_FILE="${{ env.CMAKE_TOOLCHAIN_FILE }}"
        cmake --build build --config Release
        $exe = Get-ChildItem -Path build -Recurse -Filter "bank_account.exe" | Select-Object -First 1
        if ($exe) { & $exe.FullName } else { Write-Error "bank_account.exe not found" }
        
    - name: Build HW6 
      shell: pwsh  
      run: |
        cd 2025.10.25_HW6
        cmake -B build -S .
        cmake --build build --config Release
        $exe = Get-ChildItem -Path build -Recurse -Filter "hw6.exe" | Select-Object -First 1
        if ($exe) { & $exe.FullName } else { Write-Error "hw6.exe not found" }
